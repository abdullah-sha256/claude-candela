{
  "name": "onlycans-website",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && wrangler deploy",
    "deploy:preview": "astro build && wrangler deploy --name onlycans-preview --temporary"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.1.18",
    "astro": "^5.2.0",
    "tailwindcss": "^4.1.18"
  },
  "devDependencies": {
    "wrangler": "^4.120.0"
  }
}
